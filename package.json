{
  "name": "json-server-example",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install",
    "start": "json-server --watch db.json --port 6666"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
