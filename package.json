{
  "name": "json-server-example",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install",
    "start": "json-server --watch db.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
